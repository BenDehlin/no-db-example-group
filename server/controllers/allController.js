//for the people interested in seeing a combined controller file

const food = require('./foodController')
const movies = require('./foodController')
const books = require('./foodController')
const games = require('./foodController')

module.exports = {
    food,
    movies,
    books,
    games
}